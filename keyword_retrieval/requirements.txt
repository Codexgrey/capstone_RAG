# Keyword Retrieval RAG — dependencies
# Install with: pip install -r requirements.txt

# ── Retrieval ─────────────────────────────────────────────────────
rank-bm25>=0.2.2          # BM25Okapi scoring engine

# ── NLP & Preprocessing ───────────────────────────────────────────
nltk>=3.8                  # tokenisation, stopwords, stemming
langdetect>=1.0.9          # automatic language detection

# ── Document Loading ──────────────────────────────────────────────
pymupdf>=1.23.0            # PDF text extraction  (import fitz)
python-docx>=1.1.0         # DOCX text extraction
beautifulsoup4>=4.12.0     # HTML text extraction
requests>=2.31.0           # web page fetching

# ── LLM (query normalisation + answer generation) ─────────────────
groq>=0.9.0                # Groq API client (llama-3.1-8b-instant)